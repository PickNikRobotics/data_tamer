name: ros2 lyrical

on: [push, pull_request]

jobs:
  industrial_ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: 'ros-industrial/industrial_ci@master'
        env: {ROS_DISTRO: lyrical, ROS_REPO: main}
