name: ros2 jazzy

on: [push, pull_request]

jobs:
  industrial_ci:
    name: ros2 jazzy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: 'ros-industrial/industrial_ci@master'
        env: {ROS_DISTRO: jazzy, ROS_REPO: main}
