name: ros2 humble

on: [push, pull_request]

jobs:
  industrial_ci:
    name: ros2 humble
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: 'ros-industrial/industrial_ci@master'
        env: {ROS_DISTRO: humble, ROS_REPO: main}
